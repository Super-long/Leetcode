// 比特位计数
#include <bits/stdc++.h>
using namespace std;

class Solution {// 不会dp， 直接O(n*k)就行
public:
    vector<int> countBits(int num) {

    }
};

int main(){
    Solution sol;
    auto item = sol.countBits(2);
    for(auto x : item){
        cout << x << endl;
    }
    return 0;
}